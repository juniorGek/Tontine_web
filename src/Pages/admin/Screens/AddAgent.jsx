import React from 'react'
import SideBar from '../global/SideBar'

const AddAgent = ({user}) => {
  return (
    <div>
        <SideBar user={user} >
            AddAgent
        </SideBar>
        
    </div>
  )
}

export default AddAgent